import * as chai from 'chai';
import mocha from 'mocha';
import app from '../app';

const expect = chai.expect;
const apiURL = 'http://localhost:5000'; // This is the URL until we change it

export { expect, apiURL };
